#ifndef FIELDS_KARATSUBA_H
#define FIELDS_KARATSUBA_H

unsigned Karatsuba(unsigned base, unsigned pow);

unsigned Karatsuba(unsigned base, unsigned pow){

    return 0;
};

#endif //FIELDS_KARATSUBA_H
